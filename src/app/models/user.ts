export interface IUser {
    id: string | null,
    firstName: string | null,
    lastName: string | null,
    email: string | null,
    gender: string | null,
    role: string | null,
    phoneNumber: string | null,
}